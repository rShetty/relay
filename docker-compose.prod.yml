version: '3.8'

services:
  relay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: relay-app
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      # Secure cookies are mandatory in production. The variable is pinned
      # "false" so an operator's .env cannot relax it, and config/settings.py
      # additionally refuses to boot with RELAY_ALLOW_INSECURE_COOKIES=true
      # when RELAY_ENVIRONMENT=production (fail-fast backstop).
      RELAY_ENVIRONMENT: production
      RELAY_ALLOW_INSECURE_COOKIES: "false"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - relay-network

  redis:
    image: redis:7-alpine
    container_name: relay-redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    restart: unless-stopped
    networks:
      - relay-network

networks:
  relay-network:
    driver: bridge

volumes:
  redis-data:
